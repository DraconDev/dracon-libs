# Tools

Tools are higher-level crates that can be heavier, more opinionated, or less stable.

- fine to depend on from applications/CLIs
- avoid pulling these into every repo by default
- still tested, but not part of the kernel `default-members`

